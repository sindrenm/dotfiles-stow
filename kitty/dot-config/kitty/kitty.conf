include ./kitty-scrollback-nvim.conf

font_family JetBrains Mono Nerd Font
bold_font auto
italic_font auto
bold_italic_font auto

allow_remote_control yes
listen_on unix:/tmp/kitty
shell_integration enabled

enabled_layouts tall, fat, horizontal, vertical, stack

# Tab bar styles (colored in the applied theme)
tab_bar_style powerline
tab_powerline_style slanted

# Disable Ctrl+S for flow control
map ctrl+s no_op

# Create new split panes
map alt+enter launch --location=vsplit --cwd=current
map alt+space launch --location=hsplit --cwd=current

# Navigation between panes
map alt+h neighboring_window left
map alt+j neighboring_window down
map alt+k neighboring_window up
map alt+l neighboring_window right

# Hand navigation over to smart-splits.nvim if Neovim's focused
map --when-focus-on var:IS_NVIM alt+j
map --when-focus-on var:IS_NVIM alt+k
map --when-focus-on var:IS_NVIM alt+h
map --when-focus-on var:IS_NVIM alt+l

# Scrollback and search
scrollback_lines 10000

map ctrl+s>Ctrl+s kitty_scrollback_nvim

# Tab management and navigation
map ctrl+t>n launch --type=tab --cwd=current
map ctrl+t>x close_tab
map ctrl+t>h previous_tab
map ctrl+t>l next_tab
map ctrl+t>r set_tab_title

# Toggle layouts (m for “mode” as alt+l is already taken)
map alt+m>s toggle_layout stack
map alt+m>v toggle_layout vertical
map alt+m>h toggle_layout horizontal
map alt+m>t toggle_layout tall
map alt+m>f toggle_layout fat

# “Full-screen” shortcut for toggling stack
map alt+f toggle_layout stack

# Reload config file
map alt+r load_config_file
